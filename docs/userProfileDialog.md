# `src/layouts/main-layout/common/UserProfileDialog.tsx`

Purpose:

- Dialog component that displays user profile data fetched from the Apps Script backend; includes loading and error states.

When it's used:

- Opened from `ProfileMenu` -> Account Settings.

Notes:

- Dialog backdrop is positioned below the header; the paper style is white for readability.
