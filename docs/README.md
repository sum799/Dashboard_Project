# Project Documentation (Generated)

This folder contains per-file technical documentation for the source files in `src/`.

Current scope: initial batch covering core app shell and auth-related files.

Planned next steps:
- Add documentation for remaining files in batches.
- Add line-by-line comments into source files in incremental commits.

If you want this generated differently, tell me the preferred format.
