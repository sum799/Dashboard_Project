# Routing

- `src/routes/router.tsx` — main route registry used by `RouterProvider`.
- `src/routes/paths.ts` — central path constants used across the app.
- `src/routes/sitemap.ts` — menu/sitemap definitions used for side nav (if enabled).
